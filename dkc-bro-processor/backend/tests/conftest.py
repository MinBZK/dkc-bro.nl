# import pytest
# from httpx import AsyncClient

# from app.main import app


# @pytest.fixture(scope="session")
# async def client():
#     async with AsyncClient(app=app, base_url="http://testserver/api") as async_client:
#         yield async_client
